import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth/context";
export default async function AuthLayout({ children }: { children: React.ReactNode }) { if (await getCurrentUser()) redirect("/dashboard"); return <main className="grid min-h-screen lg:grid-cols-[1fr_1.1fr]"><section className="flex items-center justify-center bg-white px-6 py-12">{children}</section><aside className="relative hidden overflow-hidden bg-emerald-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-32 -top-32 size-96 rounded-full border border-emerald-700/40" /><p className="relative text-sm font-semibold uppercase tracking-[0.2em] text-emerald-300">Built for daily farm work</p><div className="relative max-w-xl"><h2 className="text-5xl font-semibold leading-tight">One trusted place for your farm records.</h2><p className="mt-6 text-lg leading-8 text-emerald-100/75">Begin with a secure farm workspace. Production, inventory, and finance tools will grow from reliable transactions—not fragile summaries.</p></div><p className="relative text-sm text-emerald-200/70">Simple. Calm. Practical.</p></aside></main>; }
