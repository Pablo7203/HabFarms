import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{Card}from"@/components/ui/card";import{OpeningEggStockForm}from"@/components/forms/opening-egg-stock-form";
export default async function OpeningEggStockPage(){const context=await requireRole(["admin"]),supabase=await createClient(),{data:grades}=await supabase.from("egg_grades").select("id,name,system_code").eq("farm_id",context.farm.id).eq("is_active",true).order("sort_order");return <div><h1 className="text-3xl font-bold">Set Opening Egg Stock</h1><p className="mt-2 text-stone-600">Initialize an egg grade using individual eggs as the canonical quantity.</p><Card className="mt-7 p-5 sm:p-7"><OpeningEggStockForm grades={grades??[]} today={farmToday(context.farm.timezone)} crateSize={context.farm.crate_size}/></Card></div>}
