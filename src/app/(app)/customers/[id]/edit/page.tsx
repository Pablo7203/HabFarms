import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{CustomerForm}from"@/components/forms/customer-form";import{Card}from"@/components/ui/card";export default async function Edit({params}:{params:Promise<{id:string}>}){await requireRole(["admin","manager"]);const{id}=await params,s=await createClient(),{data:c}=await s.from("customers").select("*").eq("id",id).maybeSingle();if(!c)notFound();return <div><h1 className="text-3xl font-bold">Edit customer</h1><Card className="mt-7 p-6"><CustomerForm record={{id:c.id,name:c.name,phone:c.phone??"",email:c.email??"",location:c.location??"",customerType:c.customer_type,notes:c.notes??"",active:c.active,defaultCreditDays:c.default_credit_days??""}}/></Card></div>}
