import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{Card}from"@/components/ui/card";import{ExpenseForm}from"@/components/forms/health-expense-forms";export default async function NewExpense(){const c=await requireRole(["admin","manager"]),s=await createClient();const[{data:categories},{data:suppliers}]=await Promise.all([s.from("expense_categories").select("id,name").eq("farm_id",c.farm.id).eq("active",true).order("name"),s.from("suppliers").select("id,name").eq("farm_id",c.farm.id).eq("active",true).order("name")]);return <div><h1 className="text-3xl font-bold">New expense</h1><p className="mt-2 text-stone-600">Record the cost incurred and any payment made now.</p><Card className="mt-6 p-5"><ExpenseForm categories={categories??[]} suppliers={suppliers??[]} today={farmToday(c.farm.timezone)}/></Card></div>}
