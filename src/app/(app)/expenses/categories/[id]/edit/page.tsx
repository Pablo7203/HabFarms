import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";import{CategoryForm}from"@/components/forms/health-expense-forms";export default async function EditCategory({params}:{params:Promise<{id:string}>}){const c=await requireRole(["admin"]),{id}=await params,s=await createClient(),{data}=await s.from("expense_categories").select("name").eq("farm_id",c.farm.id).eq("id",id).maybeSingle();if(!data)notFound();return <div><h1 className="text-3xl font-bold">Edit expense category</h1><Card className="mt-6 p-5"><CategoryForm id={id} defaultName={data.name}/></Card></div>}
