import Link from"next/link";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";export default async function Categories(){const c=await requireRole(["admin"]),s=await createClient(),{data}=await s.from("expense_categories").select("*").eq("farm_id",c.farm.id).order("is_system",{ascending:false}).order("name");return <div><div className="flex justify-between gap-3"><h1 className="text-3xl font-bold">Expense categories</h1><Link href="/expenses/categories/new" className="rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">New category</Link></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{data?.map(x=><Card key={x.id} className="p-4"><p className="font-semibold">{x.name}</p><p className="mt-1 text-xs text-stone-500">{x.is_system?`System · ${x.system_code}`:"Custom"} · {x.active?"Active":"Inactive"}</p></Card>)}</div></div>}
