import{requireAppContext}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{Card}from"@/components/ui/card";import{HealthForm}from"@/components/forms/health-expense-forms";export default async function NewHealth(){const c=await requireAppContext(),s=await createClient(),{data}=await s.from("flocks").select("id,flock_name").eq("farm_id",c.farm.id).eq("status","active").order("flock_name");return <div><h1 className="text-3xl font-bold">New health record</h1><p className="mt-2 text-stone-600">Record operational care{c.membership.role==="worker"?". Financial fields are restricted for workers.":" and any related expense."}</p><Card className="mt-6 p-5"><HealthForm flocks={(data??[]).map(x=>({id:x.id,name:x.flock_name}))} today={farmToday(c.farm.timezone)} financial={c.membership.role!=="worker"}/></Card></div>}
