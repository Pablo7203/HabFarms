import { Card } from "@/components/ui/card";
import { SettingsForm } from "@/components/forms/settings-form";
import Link from "next/link";
import { requireRole } from "@/lib/auth/context";
import { createClient } from "@/lib/supabase/server";
export const metadata = { title: "Settings" };
export default async function SettingsPage() { const context = await requireRole(["admin"]); const supabase = await createClient(); const { data: settings } = await supabase.from("farm_settings").select("default_egg_price_per_crate,default_loose_egg_price,feed_alert_warning_days,feed_alert_critical_days,average_feed_days_window").eq("farm_id", context.farm.id).single(); if (!settings) throw new Error("Farm settings are unavailable"); return <div><div className="flex flex-wrap justify-between gap-3"><div><h1 className="text-3xl font-bold tracking-tight">Farm settings</h1><p className="mt-2 text-stone-600">Manage the defaults used throughout {context.farm.name}.</p></div><Link href="/settings/audit" className="min-h-11 rounded-lg border bg-white px-4 py-3 text-sm font-semibold">View audit trail</Link></div><Card className="mt-7 p-5 sm:p-7"><SettingsForm defaults={{ name: context.farm.name, currency: context.farm.currency, timezone: context.farm.timezone, crateSize: context.farm.crate_size, feedBagSizeKg: context.farm.feed_bag_size_kg, openingCashBalance: context.farm.opening_cash_balance, defaultEggPricePerCrate: settings.default_egg_price_per_crate, defaultLooseEggPrice: settings.default_loose_egg_price, feedAlertWarningDays: settings.feed_alert_warning_days, feedAlertCriticalDays: settings.feed_alert_critical_days, averageFeedDaysWindow: settings.average_feed_days_window }} /></Card></div>; }
