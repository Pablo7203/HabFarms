import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";import{ProductionForm}from"@/components/forms/production-form";export default async function EditProduction({params}:{params:Promise<{id:string}>}){const c=await requireRole(["admin","manager"]),{id}=await params,s=await createClient();const[{data:r},{data:flock},{data:feed}]=await Promise.all([s.from("daily_production_records").select("*").eq("id",id).maybeSingle(),s.from("v_current_flock_status").select("flock_id,flock_name,current_live_birds").eq("farm_id",c.farm.id),s.from("v_feed_forecast").select("feed_type_id,feed_type_name,quantity_kg").eq("farm_id",c.farm.id)]);if(!r)notFound();return <div><h1 className="text-3xl font-bold">Edit production</h1><p className="mt-2 text-stone-600">Feed inventory and historical cost will be reconciled atomically.</p><Card className="mt-7 p-5 sm:p-7"><ProductionForm flocks={(flock??[]).map(f=>({id:f.flock_id,flock_name:f.flock_name,current_live_birds:f.current_live_birds}))} feedTypes={(feed??[]).map(x=>({id:x.feed_type_id,name:x.feed_type_name,quantity_kg:Number(x.quantity_kg)}))} today={r.production_date} record={{id:r.id,flockId:r.flock_id,productionDate:r.production_date,eggsCollected:r.eggs_collected,crackedEggs:r.cracked_eggs,deaths:r.deaths,culls:r.culls,feedConsumedKg:r.feed_consumed_kg,feedTypeId:r.feed_type_id??"",transportCost:r.transport_cost,otherCost:r.other_cost,notes:r.notes??""}}/></Card></div>}
