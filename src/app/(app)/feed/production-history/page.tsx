/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";import { Card } from "@/components/ui/card";import { money } from "@/lib/format";import { requireAppContext } from "@/lib/auth/context";import { createClient } from "@/lib/supabase/server";export default async function ProductionHistory(){const c=await requireAppContext(),s=await createClient(),{data:batchData}=await s.from("feed_production_batches").select("id,production_date,actual_output_kg,processing_loss_kg,yield_percentage,total_material_cost,material_cost_per_kg,feed_types(name)").eq("farm_id",c.farm.id).order("production_date",{ascending:false}),data=batchData??[];return <div><div className="flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-bold">Feed Production History</h1><p className="mt-2 text-stone-600">Completed feed-production batches and their material cost.</p></div><Link href="/feed/make" className="rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">Make feed</Link></div><div className="mt-7 space-y-4">{data.map((b:any)=><Card key={b.id} className="p-5"><div className="flex flex-wrap justify-between gap-4"><div><h2 className="font-semibold">{b.feed_types?.name}</h2><p className="text-sm text-stone-600">{b.production_date}</p></div><p className="text-sm"><b>{b.actual_output_kg}kg</b> output · {b.processing_loss_kg}kg loss · {b.yield_percentage}% yield</p></div><div className="mt-3 text-sm text-stone-600">Material cost: <b>{money(b.total_material_cost,c.farm.currency)}</b> · {money(b.material_cost_per_kg,c.farm.currency)}/kg</div></Card>)}{!data.length&&<Card className="p-8 text-center text-stone-500">No feed has been made yet.</Card>}</div></div>}
