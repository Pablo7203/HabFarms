import{money}from"@/lib/format";import Link from"next/link";import{requireAppContext}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";export default async function Feed(){const c=await requireAppContext(),s=await createClient(),{data}=await s.from("v_feed_forecast").select("*").eq("farm_id",c.farm.id);return <div><div className="flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-bold">Feed inventory</h1><p className="mt-2 text-stone-600">Physical stock, historical cost and runway by feed type.</p></div><Link href="/feed/purchases/new" className="rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">Add feed purchase</Link></div><div className="mt-5 flex flex-wrap gap-2">{[["/feed/opening","Opening stock"],["/feed/adjustments/new","Adjustment"],["/feed/types","Feed types"],["/feed/suppliers","Suppliers"],["/feed/purchases","Purchase history"]].map(([h,l])=><Link key={h} href={h} className="rounded-lg border bg-white px-3 py-2 text-sm font-medium">{l}</Link>)}</div><div className="mt-7 grid gap-4 lg:grid-cols-2">{data?.map(x=><Card key={x.feed_type_id} className="p-5"><div className="flex justify-between gap-4"><div><h2 className="font-semibold">{x.feed_type_name}</h2><p className="mt-1 text-2xl font-bold">{x.quantity_kg} kg</p><p className="text-sm text-stone-500">{x.bag_equivalent} bags equivalent</p></div><span className="h-fit rounded-full bg-stone-100 px-3 py-1 text-xs font-semibold uppercase">{x.alert_level}</span></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm sm:grid-cols-4"><div><p className="text-stone-500">Avg cost</p><b>{money(x.weighted_average_cost,c.farm.currency)}</b></div><div><p className="text-stone-500">Value</p><b>{money(x.inventory_value,c.farm.currency)}</b></div><div><p className="text-stone-500">Daily use</p><b>{x.average_daily_consumption} kg</b></div><div><p className="text-stone-500">Days left</p><b>{x.days_remaining??"Unknown"}</b></div></div><p className="mt-4 text-sm text-stone-600">Estimated finish: {x.estimated_finish_date??"Unknown"}</p></Card>)}{!data?.length&&<Card className="p-8 text-center text-stone-500">Create a feed type to begin tracking feed.</Card>}</div></div>}
