import { MaterialOpeningForm } from "@/components/forms/raw-material-forms";import { requireAppContext } from "@/lib/auth/context";import { createClient } from "@/lib/supabase/server";export default async function Opening(){const c=await requireAppContext(),s=await createClient(),{data}=await s.from("raw_materials").select("id,name").eq("farm_id",c.farm.id).eq("is_active",true).order("name"),materials=data??[];return <div className="mx-auto max-w-2xl"><h1 className="text-3xl font-bold">Material opening stock</h1><div className="mt-7 rounded-xl border bg-white p-6"><MaterialOpeningForm materials={materials} today={new Date().toISOString().slice(0,10)}/></div></div>}
