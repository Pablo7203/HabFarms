import Link from"next/link";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";export default async function Suppliers(){const c=await requireRole(["admin","manager"]),s=await createClient(),{data}=await s.from("suppliers").select("*").eq("farm_id",c.farm.id).order("name");return <div><div className="flex justify-between"><h1 className="text-3xl font-bold">Feed suppliers</h1><Link href="/feed/suppliers/new" className="rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">Add supplier</Link></div><Card className="mt-6 divide-y">{data?.map(x=><Link href={`/feed/suppliers/${x.id}`} key={x.id} className="block p-5"><b>{x.name}</b><p className="text-sm text-stone-500">{x.location||"No location"} · {x.active?"Active":"Inactive"}</p></Link>)}</Card></div>}
