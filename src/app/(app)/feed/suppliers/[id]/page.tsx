import Link from"next/link";import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";export default async function Supplier({params}:{params:Promise<{id:string}>}){await requireRole(["admin","manager"]);const{id}=await params,s=await createClient(),{data}=await s.from("suppliers").select("*").eq("id",id).maybeSingle();if(!data)notFound();return <div><div className="flex justify-between"><h1 className="text-3xl font-bold">{data.name}</h1><Link href={`/feed/suppliers/${id}/edit`} className="rounded-lg border bg-white px-4 py-3">Edit</Link></div><Card className="mt-7 p-6"><p>{data.phone||"No phone"}</p><p>{data.email||"No email"}</p><p>{data.location||"No location"}</p><p className="mt-4 text-stone-600">{data.notes}</p></Card></div>}
