import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";import{PurchaseEditForm}from"@/components/forms/feed-forms";export default async function Edit({params}:{params:Promise<{id:string}>}){const c=await requireRole(["admin","manager"]),{id}=await params,s=await createClient();const[{data:p},{data:suppliers}]=await Promise.all([s.from("feed_purchases").select("*").eq("id",id).maybeSingle(),s.from("suppliers").select("id,name").eq("farm_id",c.farm.id).eq("active",true)]);if(!p)notFound();return <div><h1 className="text-3xl font-bold">Edit feed purchase</h1><p className="mt-2 text-stone-600">Inventory and downstream consumption costs will be recalculated.</p><Card className="mt-7 p-5 sm:p-7"><PurchaseEditForm id={id} suppliers={suppliers??[]} record={p}/></Card></div>}
