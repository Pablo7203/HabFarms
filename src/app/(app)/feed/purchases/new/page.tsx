import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{PurchaseForm}from"@/components/forms/feed-forms";import{Card}from"@/components/ui/card";export default async function New(){const c=await requireRole(["admin","manager"]),s=await createClient();const[{data:types},{data:suppliers}]=await Promise.all([s.from("feed_types").select("id,name,default_bag_size_kg").eq("farm_id",c.farm.id).eq("active",true),s.from("suppliers").select("id,name").eq("farm_id",c.farm.id).eq("active",true)]);return <div><h1 className="text-3xl font-bold">Add feed purchase</h1><p className="mt-2 text-stone-600">Stock and purchase liability are recorded separately from payment.</p><Card className="mt-7 p-5 sm:p-7"><PurchaseForm types={types??[]} suppliers={suppliers??[]} today={farmToday(c.farm.timezone)}/></Card></div>}
