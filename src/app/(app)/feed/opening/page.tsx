import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{OpeningForm}from"@/components/forms/feed-forms";import{Card}from"@/components/ui/card";export default async function Opening(){const c=await requireRole(["admin"]),s=await createClient(),{data}=await s.from("feed_types").select("id,name,default_bag_size_kg").eq("farm_id",c.farm.id).eq("active",true);return <div><h1 className="text-3xl font-bold">Set opening feed stock</h1><p className="mt-2 text-stone-600">One controlled historical opening balance per feed type.</p><Card className="mt-7 p-5 sm:p-7"><OpeningForm types={data??[]} today={farmToday(c.farm.timezone)}/></Card></div>}
