import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{farmToday}from"@/lib/farm-date";import{AdjustmentForm}from"@/components/forms/feed-forms";import{Card}from"@/components/ui/card";export default async function Adjustment(){const c=await requireRole(["admin"]),s=await createClient(),{data}=await s.from("feed_types").select("id,name").eq("farm_id",c.farm.id).eq("active",true);return <div><h1 className="text-3xl font-bold">Feed stock adjustment</h1><p className="mt-2 text-stone-600">Auditable correction or wastage—not a direct balance edit.</p><Card className="mt-7 p-5 sm:p-7"><AdjustmentForm types={data??[]} today={farmToday(c.farm.timezone)}/></Card></div>}
