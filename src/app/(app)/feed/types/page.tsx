import Link from"next/link";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";export default async function Types(){const c=await requireRole(["admin","manager"]),s=await createClient(),{data}=await s.from("feed_types").select("*").eq("farm_id",c.farm.id).order("name");return <div><div className="flex justify-between gap-3"><h1 className="text-3xl font-bold">Feed types</h1><Link href="/feed/types/new" className="rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">Add type</Link></div><Card className="mt-6 divide-y">{data?.map(x=><Link href={`/feed/types/${x.id}/edit`} key={x.id} className="flex justify-between p-5"><span className="font-semibold">{x.name}</span><span>{x.default_bag_size_kg} kg/bag · {x.active?"Active":"Inactive"}</span></Link>)}</Card></div>}
