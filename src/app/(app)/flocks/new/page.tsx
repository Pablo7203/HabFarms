import{requireRole}from"@/lib/auth/context";import{Card}from"@/components/ui/card";import{FlockForm}from"@/components/forms/flock-form";export const metadata={title:"New flock"};export default async function NewFlock(){await requireRole(["admin","manager"]);return <div><h1 className="text-3xl font-bold">Create flock</h1><p className="mt-2 text-stone-600">Set the batch identity and starting population.</p><Card className="mt-7 p-5 sm:p-7"><FlockForm/></Card></div>}
