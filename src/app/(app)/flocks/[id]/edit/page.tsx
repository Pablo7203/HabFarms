import{notFound}from"next/navigation";import{requireRole}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";import{Card}from"@/components/ui/card";import{FlockForm}from"@/components/forms/flock-form";export default async function EditFlock({params}:{params:Promise<{id:string}>}){await requireRole(["admin","manager"]);const{id}=await params,s=await createClient(),{data:f}=await s.from("flocks").select("*").eq("id",id).maybeSingle();if(!f)notFound();return <div><h1 className="text-3xl font-bold">Edit flock</h1><Card className="mt-7 p-5 sm:p-7"><FlockForm flock={{id:f.id,flockName:f.flock_name,batchReference:f.batch_reference??"",breed:f.breed??"",housePen:f.house_pen??"",startDate:f.start_date,initialBirds:f.initial_birds,ageAtArrivalWeeks:f.age_at_arrival_weeks??"",source:f.source??"",notes:f.notes??"",status:f.status}}/></Card></div>}
