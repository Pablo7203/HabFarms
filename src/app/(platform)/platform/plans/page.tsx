import { createClient } from "@/lib/supabase/server";
import { Card } from "@/components/ui/card";

export default async function PlatformPlans() { const supabase = await createClient(); const { data } = await supabase.from("subscription_plans").select("*").order("name"); return <div><h1 className="text-3xl font-bold">Subscription plans</h1><p className="mt-2 text-stone-600">Phase 1 plans are commercial foundations. Pricing and access enforcement are configured in later phases.</p><div className="mt-7 grid gap-4 lg:grid-cols-3">{data?.map((plan) => <Card key={plan.id} className="p-6"><p className="text-xs font-semibold tracking-[0.12em] text-emerald-800">{plan.code}</p><h2 className="mt-2 text-xl font-bold">{plan.name}</h2><p className="mt-2 min-h-10 text-sm text-stone-600">{plan.description}</p><dl className="mt-5 space-y-2 text-sm"><div className="flex justify-between gap-3"><dt className="text-stone-500">Trial</dt><dd>{plan.default_trial_days} days</dd></div><div className="flex justify-between gap-3"><dt className="text-stone-500">Monthly price</dt><dd>{plan.monthly_price == null ? "To be configured" : `${plan.currency} ${plan.monthly_price}`}</dd></div><div className="flex justify-between gap-3"><dt className="text-stone-500">Status</dt><dd>{plan.is_active ? "Active" : "Inactive"}</dd></div></dl></Card>)}</div></div>; }
