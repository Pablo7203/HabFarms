import Link from "next/link";
import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { Card } from "@/components/ui/card";
import { OwnerInvitationActions } from "@/components/forms/platform-farms";

export default async function PlatformFarmDetail({ params }: { params: Promise<{ farmId: string }> }) {
  const { farmId } = await params, supabase = await createClient(); const { data } = await supabase.rpc("platform_get_farm_detail", { target_farm: farmId }); const farm = data?.[0]; if (!farm) notFound();
  const milestones = [["Account", farm.account_completed_at, "Completed when the owner accepted the invitation."], ["Farm settings", farm.farm_settings_completed_at, "Required before normal dashboard access."], ["First flock", farm.first_flock_completed_at, "Recommended; may be skipped for a new farm."], ["Opening stock", farm.opening_stock_completed_at, "Optional; always uses the existing ledgers."]];
  return <div><div className="flex flex-wrap items-start justify-between gap-4"><div><Link href="/platform/farms" className="text-sm font-semibold text-emerald-800">← Back to customer farms</Link><h1 className="mt-3 text-3xl font-bold">{farm.farm_name}</h1><p className="mt-2 capitalize text-stone-600">{farm.account_status} · {farm.plan_name ?? "No plan"} · {farm.subscription_status}</p></div></div><div className="mt-7 grid gap-5 lg:grid-cols-2"><Card className="p-6"><h2 className="font-semibold">Customer account</h2><dl className="mt-4 space-y-3 text-sm"><div><dt className="text-stone-500">Primary owner</dt><dd className="font-medium">{farm.contact_name}</dd></div><div><dt className="text-stone-500">Email</dt><dd>{farm.contact_email}</dd></div><div><dt className="text-stone-500">Phone</dt><dd>{farm.contact_phone || "—"}</dd></div><div><dt className="text-stone-500">Country</dt><dd>{farm.country || "—"}</dd></div></dl></Card><Card className="p-6"><h2 className="font-semibold">Initial subscription</h2><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-stone-500">Plan</dt><dd className="font-medium">{farm.plan_name ?? "—"}</dd></div><div><dt className="text-stone-500">Billing cycle</dt><dd className="capitalize">{farm.billing_cycle ?? "—"}</dd></div><div><dt className="text-stone-500">Status</dt><dd className="capitalize">{farm.subscription_status ?? "—"}</dd></div><div><dt className="text-stone-500">Trial ends</dt><dd>{farm.trial_ends_at ?? "—"}</dd></div></dl></Card><Card className="p-6"><h2 className="font-semibold">Onboarding</h2><p className="mt-1 text-sm text-stone-600">{farm.onboarding_status === "completed" ? "Completed" : `Current step: ${farm.current_step.replace("_", " ")}`}</p><div className="mt-4 divide-y">{milestones.map(([label, timestamp, help]) => <div key={String(label)} className="flex items-start justify-between gap-4 py-3 text-sm"><div><p className="font-medium">{label}</p><p className="text-stone-500">{help}</p></div><span className={timestamp ? "text-emerald-800" : "text-stone-500"}>{timestamp ? "Complete" : "Not completed"}</span></div>)}</div></Card><Card className="p-6"><h2 className="font-semibold">Owner invitation</h2><dl className="mt-4 grid gap-3 text-sm"><div><dt className="text-stone-500">Status</dt><dd className="capitalize">{farm.invitation_status ?? "—"}</dd></div><div><dt className="text-stone-500">Delivery</dt><dd className="capitalize">{farm.invitation_delivery_status ?? "—"}</dd></div><div><dt className="text-stone-500">Expires</dt><dd>{farm.invitation_expires_at ? new Date(farm.invitation_expires_at).toLocaleString() : "—"}</dd></div></dl>{farm.invitation_id && <OwnerInvitationActions invitationId={farm.invitation_id} canResend={farm.invitation_status === "pending" || farm.invitation_status === "expired"} canRevoke={farm.invitation_status === "pending" || farm.invitation_status === "expired"} />}</Card></div><Card className="mt-5 p-6"><h2 className="font-semibold">Privacy boundary</h2><p className="mt-2 text-sm text-stone-600">This page intentionally contains tenant/account metadata only. It does not expose egg sales, expenses, customer debt, feed costs, health records, or farm profitability.</p><p className="mt-3 text-sm text-stone-600">Active farm users: <span className="font-semibold text-stone-900">{farm.user_count}</span></p></Card></div>;
}
