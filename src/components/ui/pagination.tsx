import Link from "next/link";
export const PAGE_SIZE=50;
export function pageWindow(value?:string){const page=Math.max(1,Number(value)||1);return{page,from:(page-1)*PAGE_SIZE,to:page*PAGE_SIZE-1}}
export function Pagination({page,hasMore,base,params={}}:{page:number;hasMore:boolean;base:string;params?:Record<string,string|undefined>}){const href=(target:number)=>`${base}?${new URLSearchParams({...Object.fromEntries(Object.entries(params).filter(([,v])=>v)),page:String(target)}).toString()}`;return <nav aria-label="Pagination" className="mt-5 flex items-center justify-between"><Link aria-disabled={page===1} href={page===1?href(1):href(page-1)} className="min-h-11 rounded-lg border bg-white px-4 py-3 aria-disabled:pointer-events-none aria-disabled:opacity-50">Previous</Link><span className="text-sm">Page {page}</span><Link aria-disabled={!hasMore} href={hasMore?href(page+1):href(page)} className="min-h-11 rounded-lg border bg-white px-4 py-3 aria-disabled:pointer-events-none aria-disabled:opacity-50">Next</Link></nav>}
