"use client";
import{useState,useTransition}from"react";import{useRouter}from"next/navigation";import{updateSaleCreditTermsAction}from"@/app/actions/sales";import{Input}from"@/components/ui/input";import{Button}from"@/components/ui/button";
export function CreditTermsForm({saleId,saleDate,creditDays,dueDate}:{saleId:string;saleDate:string;creditDays:number|null;dueDate:string|null}){const[days,setDays]=useState(creditDays??7),[due,setDue]=useState(dueDate??""),[reason,setReason]=useState(""),[message,setMessage]=useState(""),[pending,start]=useTransition(),router=useRouter();const recalc=(v:number)=>{setDays(v);const d=new Date(`${saleDate}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+v);setDue(d.toISOString().slice(0,10))};return <form className="mt-4 grid gap-4 sm:grid-cols-2" onSubmit={e=>{e.preventDefault();start(async()=>{const x=await updateSaleCreditTermsAction(saleId,{creditDays:days,dueDate:due,reason});setMessage(x.message);if(x.ok)router.refresh()})}}><label className="text-sm font-medium">Credit days<Input className="mt-2" type="number" min="1" max="365" value={days} onChange={e=>recalc(Number(e.target.value))}/></label><label className="text-sm font-medium">Due date<Input className="mt-2" type="date" min={saleDate} value={due} onChange={e=>{setDue(e.target.value);const delta=Math.round((Date.parse(`${e.target.value}T12:00:00Z`)-Date.parse(`${saleDate}T12:00:00Z`))/86400000);setDays(delta)}}/></label><label className="text-sm font-medium sm:col-span-2">Change reason<Input className="mt-2" required minLength={3} value={reason} onChange={e=>setReason(e.target.value)} placeholder="Customer requested an extension"/></label>{message&&<p role="status" className="text-sm sm:col-span-2">{message}</p>}<div className="sm:col-span-2"><Button disabled={pending}>{pending?"Saving...":dueDate?"Update credit terms":"Set due date"}</Button></div></form>}
