"use client";import{useState,useTransition}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{createCashAdjustmentAction,voidCashAdjustmentAction}from"@/app/actions/reporting";import{VoidDialog}from"@/components/forms/sprint5-mutations";export function CashAdjustmentForm({today}:{today:string}){const[pending,start]=useTransition(),[message,setMessage]=useState(""),router=useRouter();return <form className="space-y-5" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);start(async()=>{const type=String(f.get("type")),direction=type==="correction"?f.get("direction"):type.includes("withdrawal")||type.includes("outflow")?"OUT":"IN",r=await createCashAdjustmentAction({date:f.get("date"),type,direction,amount:f.get("amount"),method:f.get("method"),description:f.get("description"),reference:f.get("reference"),notes:f.get("notes")});setMessage(r.message);if(r.ok)router.push("/cash-flow")})}}><div className="grid gap-5 sm:grid-cols-2"><label>Date<Input name="date" type="date" defaultValue={today}/></label><label>Adjustment type<select name="type" className="mt-2 min-h-11 w-full rounded-lg border px-3"><option value="owner_capital">Owner capital</option><option value="owner_withdrawal">Owner withdrawal</option><option value="other_inflow">Other inflow</option><option value="other_outflow">Other outflow</option><option value="correction">Correction</option></select></label><label>Correction direction<select name="direction" className="mt-2 min-h-11 w-full rounded-lg border px-3"><option value="IN">Inflow</option><option value="OUT">Outflow</option></select></label><label>Payment method<select name="method" className="mt-2 min-h-11 w-full rounded-lg border px-3"><option value="cash">Cash</option><option value="momo">MoMo</option><option value="bank_transfer">Bank transfer</option><option value="other">Other</option></select></label><label>Amount<Input name="amount" type="number" step="0.01"/></label><label>Description<Input name="description"/></label><label>Reference<Input name="reference"/></label></div><label className="block">Notes<textarea name="notes" className="mt-2 min-h-24 w-full rounded-lg border p-3"/></label>{message&&<p role="status" className="rounded-lg bg-stone-100 p-3 text-sm">{message}</p>}<Button disabled={pending}>{pending?"Saving…":"Record cash adjustment"}</Button></form>}

export const CashVoidButton=({id}:{id:string})=><VoidDialog label="Void adjustment" title="Void this cash adjustment?" description="The adjustment remains in history and will no longer affect tracked cash." action={r=>voidCashAdjustmentAction(id,r)}/>;