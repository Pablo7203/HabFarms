"use client";import{useState,useTransition}from"react";import{useForm,type Resolver}from"react-hook-form";import{zodResolver}from"@hookform/resolvers/zod";import{paymentSchema}from"@/lib/validation/sales";import{recordPaymentAction}from"@/app/actions/sales";import{Input}from"@/components/ui/input";import{Button}from"@/components/ui/button";type V={paymentDate:string;amount:number|string;paymentMethod:"cash"|"momo"|"bank_transfer"|"other";reference:string;notes:string};export function PaymentForm({saleId,today,balance}:{saleId:string;today:string;balance:number}){const[pending,start]=useTransition(),[status,setStatus]=useState("");const{register,handleSubmit,reset}=useForm<V>({resolver:zodResolver(paymentSchema)as unknown as Resolver<V>,defaultValues:{paymentDate:today,amount:balance,paymentMethod:"cash",reference:"",notes:""}});return <form className="grid gap-4 sm:grid-cols-2" onSubmit={handleSubmit(v=>start(async()=>{const r=await recordPaymentAction(saleId,v);setStatus(r.message);if(r.ok)reset({...v,amount:""})}))}><label className="text-sm">Date<Input className="mt-2" type="date" {...register("paymentDate")}/></label><label className="text-sm">Amount<Input className="mt-2" type="number" min="0.01" step="0.01" {...register("amount")}/></label><label className="text-sm">Method<select className="mt-2 min-h-11 w-full rounded-lg border px-3" {...register("paymentMethod")}><option value="cash">Cash</option><option value="momo">Mobile money</option><option value="bank_transfer">Bank transfer</option><option value="other">Other</option></select></label><label className="text-sm">Reference<Input className="mt-2" {...register("reference")}/></label>{status&&<p className="sm:col-span-2 text-sm">{status}</p>}<div className="sm:col-span-2"><Button disabled={pending}>{pending?"Saving...":"Record payment"}</Button></div></form>}
