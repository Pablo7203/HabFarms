import Link from"next/link";import{Card}from"@/components/ui/card";export function DateFilter({from,to,children,exportType}:{from:string;to:string;children?:React.ReactNode;exportType?:string}){return <form className="mt-6 grid gap-3 rounded-2xl border bg-white p-4 sm:grid-cols-2 lg:grid-cols-5"><label className="text-sm font-medium">From<input name="from" type="date" defaultValue={from} className="mt-2 min-h-11 w-full rounded-lg border px-3"/></label><label className="text-sm font-medium">To<input name="to" type="date" defaultValue={to} className="mt-2 min-h-11 w-full rounded-lg border px-3"/></label>{children}<button className="min-h-11 self-end rounded-lg border px-4 text-sm font-semibold">Apply filters</button>{exportType&&<Link href={`/reports/export?type=${exportType}&from=${from}&to=${to}`} className="inline-flex min-h-11 items-center justify-center self-end rounded-lg border px-4 text-sm font-semibold">Export CSV</Link>}</form>}
export function Kpis({items}:{items:Array<[string,string|number,string?]>}){return <div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{items.map(([l,v,h])=><Card key={l} className="p-5"><p className="text-sm text-stone-500">{l}</p><p className="mt-2 break-words text-2xl font-bold">{v}</p>{h&&<p className="mt-2 text-xs text-stone-500">{h}</p>}</Card>)}</div>}
export function ReportTable({headers,rows,empty}:{headers:string[];rows:Array<Array<React.ReactNode>>;empty:string}){return <Card className="mt-6 overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-stone-100"><tr>{headers.map(x=><th scope="col" className="p-3 font-semibold" key={x}>{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map((r,i)=><tr key={i}>{r.map((x,j)=><td className="p-3 align-top" key={j}>{x}</td>)}</tr>)}</tbody></table></div>{!rows.length&&<p className="p-8 text-center text-stone-500">{empty}</p>}</Card>}
export function SimpleChart({title,series,unit=""}:{title:string;series:Array<{label:string;value:number}>;unit?:string}){const max=Math.max(...series.map(x=>Math.abs(x.value)),1);return <Card className="mt-6 p-5"><h2 className="font-semibold">{title}</h2>{series.length?<div role="img" aria-label={`${title}: ${series.map(x=>`${x.label} ${x.value} ${unit}`).join(", ")}`} className="mt-5 space-y-3">{series.map(item=><div key={item.label} className="grid grid-cols-[8rem_1fr_7rem] items-center gap-3 text-sm"><span>{item.label}</span><div className="h-5 rounded bg-stone-100"><div className="h-5 rounded bg-emerald-700" style={{width:`${Math.max(2,Math.abs(item.value)/max*100)}%`}}/></div><span className="text-right font-medium">{item.value.toFixed(2)}{unit&&` ${unit}`}</span></div>)}</div>:<p className="mt-5 text-sm text-stone-500">No chart data for this period.</p>}</Card>}
