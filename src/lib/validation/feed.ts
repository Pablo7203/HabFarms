import { z } from "zod";
const n=z.coerce.number(),text=z.string().trim().max(2000).optional().default("");
export const feedTypeSchema=z.object({name:z.string().trim().min(2).max(120),defaultBagSizeKg:n.positive(),description:text,active:z.boolean().optional().default(true)});
export const supplierSchema=z.object({name:z.string().trim().min(2).max(160),phone:text,email:z.union([z.literal(""),z.email()]).optional().default(""),location:text,notes:text,suppliedFeedTypeIds:z.array(z.uuid()).default([]),suppliedMaterialIds:z.array(z.uuid()).default([]),active:z.boolean().optional().default(true)});
export const rawMaterialSchema=z.object({name:z.string().trim().min(2).max(160),description:text,packageLabel:z.string().trim().max(40).optional().default(""),defaultPackageWeightKg:z.union([z.literal(""),n.positive()]).optional().default("")});
export const materialOpeningSchema=z.object({materialId:z.uuid(),effectiveDate:z.iso.date(),quantityKg:n.positive(),unitCost:n.nonnegative(),notes:text});
export const materialPurchaseSchema=z.object({materialId:z.uuid(),supplierId:z.union([z.literal(""),z.uuid()]),purchaseDate:z.iso.date(),quantityKg:n.positive(),unitCost:n.nonnegative(),amountPaid:n.nonnegative(),notes:text});
export const materialPurchaseBatchSchema=z.object({supplierId:z.union([z.literal(""),z.uuid()]),purchaseDate:z.iso.date(),amountPaid:n.nonnegative(),notes:text,items:z.array(z.object({materialId:z.uuid(),quantityKg:n.positive(),unitCost:n.nonnegative(),packageCount:z.union([z.literal(""),n.positive()]).default(""),packageWeightKg:z.union([z.literal(""),n.positive()]).default("")})).min(1)}).superRefine((value,context)=>{const ids=value.items.map(item=>item.materialId);if(new Set(ids).size!==ids.length)context.addIssue({code:"custom",message:"Each material can appear only once in a purchase."});});
export const recipeSchema=z.object({feedTypeId:z.uuid(),effectiveFrom:z.iso.date(),basisKg:n.positive().max(100000),notes:text,items:z.array(z.object({materialId:z.uuid(),quantityKg:n.positive()})).min(1)});
export const feedProductionSchema=z.object({feedTypeId:z.uuid(),productionDate:z.iso.date(),plannedOutputKg:n.positive(),actualOutputKg:n.positive(),notes:text,items:z.array(z.object({materialId:z.uuid(),quantityKg:n.positive()})).min(1)});
export const purchaseSchema=z.object({supplierId:z.union([z.literal(""),z.uuid()]),feedTypeId:z.uuid(),purchaseDate:z.iso.date(),bags:n.positive(),bagSizeKg:n.positive(),costPerBag:n.nonnegative(),amountPaid:n.nonnegative(),paymentMethod:z.enum(["cash","momo","bank_transfer","other"]),reference:text,notes:text});
export const openingSchema=z.object({feedTypeId:z.uuid(),effectiveDate:z.iso.date(),bags:n.positive(),bagSizeKg:n.positive(),costPerBag:n.nonnegative(),notes:text});
export const adjustmentSchema=z.object({feedTypeId:z.uuid(),movementDate:z.iso.date(),movementType:z.enum(["adjustment","wastage"]),direction:z.enum(["IN","OUT"]),quantityKg:n.positive(),unitCost:z.union([z.literal(""),n.nonnegative()]),reason:z.string().trim().min(3).max(300),notes:text});
export const feedPaymentSchema=z.object({paymentDate:z.iso.date(),amount:n.positive(),paymentMethod:z.enum(["cash","momo","bank_transfer","other"]),reference:text,notes:text});
