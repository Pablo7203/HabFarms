import{z}from"zod";export const cashAdjustmentSchema=z.object({date:z.iso.date(),type:z.enum(["owner_capital","owner_withdrawal","other_inflow","other_outflow","correction"]),direction:z.enum(["IN","OUT"]),amount:z.coerce.number().positive(),method:z.enum(["cash","momo","bank_transfer","other"]),description:z.string().trim().min(2).max(300),reference:z.string().trim().optional(),notes:z.string().trim().optional()});
